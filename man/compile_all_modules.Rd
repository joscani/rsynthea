% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{compile_all_modules}
\alias{compile_all_modules}
\title{Compile GMF modules for the C++ simulation engine}
\usage{
compile_all_modules(modules_r)
}
\arguments{
\item{modules_r}{Named list of \code{Module} objects as returned by
\code{\link[=load_all_modules]{load_all_modules()}}.}
}
\value{
An external pointer (\verb{XPtr<vector<CppModule>>}) to the compiled
module set. Pass this to \code{generate_population(cpp_modules = ...)} to
reuse the compiled modules across multiple calls without recompiling.
}
\description{
Converts a list of R \code{Module} objects (from \code{\link[=load_all_modules]{load_all_modules()}}) into
compiled C++ structs held in an external pointer. The compilation step
takes ~0.2 s and needs to be done only once per session.
}
\details{
You rarely need to call this directly. \code{\link[=generate_population]{generate_population()}} compiles and
caches modules automatically on the first call. Use \code{compile_all_modules()}
explicitly only when working with custom modules (loaded via
\code{\link[=load_all_modules]{load_all_modules()}}) that should bypass the session cache.

The returned pointer is valid for the lifetime of the R session. It becomes
invalid after \code{\link[=rsynthea_clear_cache]{rsynthea_clear_cache()}} is called or after reloading the
package.
}
\examples{
\dontrun{
# Manual workflow (only needed for custom modules)
m     <- load_all_modules("path/to/custom/modules")
cpp_m <- compile_all_modules(m)
tbls  <- generate_population(100, seed = 1L,
                             end_date  = as.POSIXct("2020-01-01"),
                             modules   = m,
                             cpp_modules = cpp_m)
}

}
\seealso{
\code{\link[=load_all_modules]{load_all_modules()}}, \code{\link[=generate_population]{generate_population()}}, \code{\link[=rsynthea_clear_cache]{rsynthea_clear_cache()}}
}
